import type { ServerFleet, ServerInfo } from '../Api/index.js';
import type { Group } from '../Group/index.js';
import { TypedEmitter } from '@mdingena/tiny-typed-emitter';
import { ServerConnection } from '../ServerConnection/index.js';
type Player = {
    id: number;
    username: string;
};
interface Events {
    connect: (serverConnection: ServerConnection) => void;
    update: (server: Server) => void;
}
export declare class Server extends TypedEmitter<Events> {
    description: string;
    group: Group;
    id: number;
    name: string;
    playability: number;
    players: Player[];
    status: 'disconnected' | 'connecting' | 'connected';
    fleet: ServerFleet;
    private connection?;
    private keepAlive;
    private missedHeartbeats;
    constructor(group: Group, server: ServerInfo);
    connect(): Promise<ServerConnection>;
    disconnect(): void;
    private reconnect;
    update(status: ServerInfo): void;
    handleHeartbeat(status: ServerInfo): void;
    dispose(): void;
}
export {};
